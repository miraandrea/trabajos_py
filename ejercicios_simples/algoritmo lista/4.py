#Pedir por teclado el tamaño de una lista de números y pedir los valores numéricos con los que se 
# rellena. Los valores no se pueden repetir. Mostrar el arreglo con los valores al final.
lista = []
n = int(input("valor de tamaño"))
for e in range(n):
    e= int(input("numero:"))
    lista.append(e)
    len(e)
    print("no se puede repetir numero")
print(lista)